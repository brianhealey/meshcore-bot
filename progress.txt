# LLM Tool Calling Integration - Progress Log

## Codebase Patterns
- Database migrations go in modules/db_migrations.py with sequential IDs
- Migrations use helper functions like _add_column() for safe column additions
- Each migration is a function named _mNNNN_description(cursor)
- Migrations are registered in MIGRATIONS list as (version, description, function)
- Never modify existing migrations - always add new ones
- Use _column_exists() to make migrations idempotent
- Test migrations by adding tests in tests/test_db_migrations.py
- Use tmp_path fixture for test databases (not :memory:)
- Typecheck with: .venv/bin/mypy modules/db_migrations.py --config-file=pyproject.toml
- Lint with: .venv/bin/ruff check modules/

---

## 2026-07-04 - US-001
- **What was implemented:**
  - Added migration _m0014_extend_llm_context_for_commands to extend llm_conversation_context table
  - Added command_name column (nullable TEXT) to track which command was executed
  - Added sender_name column (nullable TEXT) to support user mentions in responses
  - Registered migration as version 14 in MIGRATIONS list
  - Added two test cases to verify columns are created after migration

- **Files changed:**
  - modules/db_migrations.py: Added _m0014_extend_llm_context_for_commands migration function
  - modules/db_migrations.py: Added migration entry to MIGRATIONS list
  - tests/test_db_migrations.py: Added test_llm_conversation_context_has_command_name
  - tests/test_db_migrations.py: Added test_llm_conversation_context_has_sender_name

- **Learnings for future iterations:**
  - Migration helper functions (_add_column, _column_exists) make it easy to add nullable columns safely
  - The pattern is: define migration function, register in MIGRATIONS list, add test
  - Tests use runner.run() to apply all migrations, then check column existence with _column_exists()
  - Migration 13 created the base llm_conversation_context table, migration 14 extends it
  - Migration numbering is sequential and must never be skipped or reused
  - Typecheck and linting both passed without any issues

---

## 2026-07-04 - US-002
- **What was implemented:**
  - Added user mention support to LLMCommand responses
  - Implemented _add_user_mention() helper method in llm_command.py
  - Added include_user_mention config loading in __init__ (default: true)
  - Modified execute() to prefix all responses with "[@username] " when enabled
  - Automatically skips mention for DMs
  - Updated config.ini.example with include_user_mention setting and documentation
  - Added 5 comprehensive tests for user mention functionality

- **Files changed:**
  - modules/commands/llm_command.py: Added include_user_mention config, _add_user_mention() method
  - modules/commands/llm_command.py: Updated all response paths to use mention helper
  - config.ini.example: Added include_user_mention setting with comments
  - tests/test_llm_command.py: Added TestLLMCommandUserMention class with 5 test cases

- **Learnings for future iterations:**
  - MeshMessage.sender_id contains the user's name/identifier (not just an ID)
  - User mentions should be prefixed to chunked responses BEFORE chunking to avoid splitting
  - All response paths (success, error, clear-context) need mention support for consistency
  - DM detection uses message.is_dm boolean flag
  - Testing strategy: test enabled/disabled, channel/DM, and all response types
  - Config defaults should favor user-friendly features (default: true for mentions)

---

## 2026-07-04 - US-003
- **What was implemented:**
  - Extended LLMContextManager to support command context tracking
  - Added add_command_context() method to store command name, user input, bot response, and sender name
  - Updated get_context() to return command_name and sender_name fields
  - Updated format_context_for_ollama() documentation to reflect command context support
  - Added track_all_commands config option (default: true) to LLMCommand.__init__
  - Updated config.ini.example with comprehensive documentation
  - Added 7 comprehensive tests covering all new functionality

- **Files changed:**
  - modules/llm_context_manager.py: Added add_command_context() method
  - modules/llm_context_manager.py: Updated get_context() to SELECT new fields
  - modules/llm_context_manager.py: Updated format_context_for_ollama() docstring
  - modules/commands/llm_command.py: Added track_all_commands config loading
  - config.ini.example: Added track_all_commands setting with examples
  - tests/test_llm_context_manager.py: Added TestAddCommandContext class (5 tests)
  - tests/test_llm_context_manager.py: Added TestCommandContextFields class (2 tests)

- **Learnings for future iterations:**
  - Command context uses same timestamp approach as regular messages (user + 0.001 for assistant)
  - Migration 14 added command_name and sender_name columns - use them in SELECT queries
  - add_command_context() creates TWO entries: user role (command input) + assistant role (bot response)
  - format_context_for_ollama() doesn't need changes - it only extracts role and content
  - Test ordering may vary due to timestamps - filter by command_name rather than index
  - Command context enables LLM to reference previous bot interactions (wx, path, airplanes, etc.)
  - Both command context and regular LLM messages can coexist in same context_key

---
## 2026-07-04 - US-004
- **What was implemented:**
  - Added command context capture hook in CommandManager.execute_commands
  - Hook triggers after successful command execution (after web viewer integration block)
  - Checks if track_all_commands is enabled via LLM command instance
  - Extracts context_key using same logic as LLMCommand (channel name or sender pubkey)
  - Calls LLMContextManager.add_command_context() with command name, user input, bot response, and sender name
  - Skips !ask and !clear-context commands (already handled by LLMCommand)
  - Only captures context when response was sent and command succeeded
  - Handles errors gracefully with debug logging
  - Added 11 comprehensive tests covering all context capture scenarios

- **Files changed:**
  - modules/command_manager.py: Added context capture hook after line 1841
  - tests/test_command_context_capture.py: New test file with 11 test cases

- **Learnings for future iterations:**
  - Command execution flow: execute_commands() → command.execute() → check response → stats → web viewer → context capture
  - Context capture happens AFTER web viewer integration to ensure response is fully tracked
  - Access LLM command via self.commands.get('ask') to check track_all_commands flag
  - Mock commands need: is_channel_allowed, should_execute, get_response_format, cooldown_seconds
  - Mock commands must return None from get_response_format() to be treated as self-handling
  - CommandManager accesses command response via command.last_response or self._last_response
  - Context capture is opt-in via track_all_commands config (default: true)
  - Context capture failures are logged but don't break command execution (graceful degradation)
  - All 2981 tests pass, typecheck passes, lint passes

---

## 2026-07-04 - US-005
- **What was implemented:**
  - Added chat() async method to OllamaClient class for tool calling support
  - Method accepts messages (list of dicts), tools (optional list), and stream (bool) parameters
  - Calls /api/chat endpoint with tool definitions in OpenAI function format
  - Returns full response dict including message with optional tool_calls array
  - Handles errors and timeouts consistently with existing generate() method
  - Added 10 comprehensive tests covering all chat() scenarios

- **Files changed:**
  - modules/ollama_client.py: Added chat() method (76 lines)
  - tests/test_ollama_client.py: Added TestChat class with 10 test cases

- **Learnings for future iterations:**
  - chat() returns full response dict (not just content string like generate())
  - Response includes: message (dict with role, content, optional tool_calls), done (bool), model (str)
  - Tools parameter is optional - only included in payload if provided
  - Stream parameter defaults to False (consistent with generate())
  - Error handling pattern: validate response has 'message' field, then validate it's a dict
  - Tool calls are returned as part of message dict: message["tool_calls"] if present
  - All 3005 tests pass, typecheck passes, lint passes
  - OllamaClient now supports both generate() for simple text and chat() for tool calling

---

## 2026-07-04 - US-006
- **What was implemented:**
  - Created ToolRegistry class for discovering and converting bot commands into LLM tool schemas
  - Implemented get_available_commands() to filter commands by whitelist from config
  - Implemented generate_tool_schema() to convert command metadata to OpenAI function format
  - Implemented get_all_tool_schemas() to return all available tool schemas
  - Reads available_tools from [LLM_Command] config section (comma-separated list)
  - Added 16 comprehensive tests covering all ToolRegistry functionality
  - Updated config.ini.example with available_tools setting and documentation

- **Files changed:**
  - modules/tool_registry.py: New module with ToolRegistry class (119 lines)
  - tests/test_tool_registry.py: New test file with 16 test cases
  - config.ini.example: Added available_tools setting with documentation

- **Learnings for future iterations:**
  - Commands expose metadata via class attributes: name, description, short_description, parameters
  - Parameters list contains dicts with: name, description, required, type, optional enum
  - OpenAI function schema format: type=function, function={name, description, parameters}
  - Parameters object has: type=object, properties={}, required=[] array
  - short_description is preferred over description for tool schemas (more concise)
  - Config whitelist pattern: comma-separated string → split → strip → set comprehension
  - ToolRegistry bridges command system and LLM tool calling by converting metadata to schemas
  - Default whitelist: wx,airplanes,satpass,path,stats,moon,sun,aurora
  - All 3021 tests pass, typecheck passes, lint passes

---

## 2026-07-04 - US-007
- **What was implemented:**
  - Created ToolExecutor class for executing LLM tool calls by routing through CommandManager
  - Implemented execute_tool() method that creates synthetic MeshMessages from tool arguments
  - Added timeout handling (default: 10 seconds, configurable)
  - Validates tool_name against ToolRegistry whitelist before execution
  - Captures command output from command.last_response or command_manager._last_response
  - Handles errors gracefully and returns descriptive error messages to LLM
  - Builds message content from tool arguments, respecting command parameter order
  - Added 20 comprehensive tests covering all execution scenarios

- **Files changed:**
  - modules/tool_executor.py: New module with ToolExecutor class (170 lines)
  - tests/test_tool_executor.py: New test file with 20 test cases

- **Learnings for future iterations:**
  - ToolExecutor creates synthetic MeshMessages that preserve original message context (sender, channel, etc.)
  - Command output is captured via last_response attribute after execution
  - AsyncMock objects need explicit attribute initialization (parameters = []) to avoid auto-mocking
  - execute_tool() waits 0.1 seconds after command execution for send_response to complete
  - Message content is built from arguments using command.parameters metadata for proper ordering
  - Timeout errors and exceptions are caught and returned as strings for LLM to interpret
  - Whitelist validation ensures only approved commands can be called by LLM
  - All 3041 tests pass, typecheck passes, lint passes

---

## 2026-07-04 - US-008
- **What was implemented:**
  - Added tool calling integration to LLMCommand for executing commands via LLM function calling
  - Added enable_tools, max_tools_per_query, and tool_timeout config options to LLMCommand.__init__
  - Initialize ToolRegistry and ToolExecutor when enable_tools is true
  - Implemented _execute_with_tools() method with tool-calling loop
  - Loop: call chat() → check tool_calls → execute via ToolExecutor → add results to messages → repeat
  - Respects max_tools_per_query limit (default: 3) to prevent abuse
  - Handles tool execution errors gracefully and returns error messages to LLM
  - Updated config.ini.example with enable_tools, max_tools_per_query, tool_timeout settings
  - Added 10 comprehensive tests for tool-calling flow

- **Files changed:**
  - modules/commands/llm_command.py: Added tool calling config, imports, initialization, _execute_with_tools method
  - modules/commands/llm_command.py: Modified execute() to use chat() when enable_tools is true
  - config.ini.example: Added enable_tools, max_tools_per_query, tool_timeout settings with documentation
  - tests/test_llm_command.py: Added TestLLMCommandToolCalling class with 10 test cases

- **Learnings for future iterations:**
  - ToolRegistry and ToolExecutor expect bot as first parameter, not separate config/logger
  - tool_executor.execute_tool() takes timeout as parameter, not in __init__
  - Type annotations for Optional types: use `ToolRegistry | None` instead of `Optional[ToolRegistry]`
  - chat() returns full response dict with message.tool_calls array, not just content string
  - Tool calling loop must handle multiple iterations until LLM returns final response
  - Max iterations prevents infinite loops if LLM keeps requesting tools
  - Tool results are added as {"role": "tool", "content": result} messages
  - Final call should disable tools (tools=None) to force LLM to provide answer
  - All 3037 tests pass, typecheck passes, 44.53% coverage

---

## 2026-07-04 - US-009
- **What was implemented:**
  - Added parameter metadata to WxCommand with required/type/enum fields
  - location parameter is required (string type)
  - forecast_type parameter is optional with enum values: current, tomorrow, 7d, hourly, alerts
  - Changed parameter name from "option" to "forecast_type" for better LLM understanding
  - Added 4 tests in TestWxCommandToolSchema class to verify parameter structure
  - Added 4 tests in TestWxCommandToolExecution class to verify tool execution with various forecast types

- **Files changed:**
  - modules/commands/wx_command.py: Updated parameters list with required/type/enum metadata
  - tests/test_tool_registry.py: Added TestWxCommandToolSchema class with 4 test cases
  - tests/test_tool_executor.py: Added TestWxCommandToolExecution class with 4 test cases

- **Learnings for future iterations:**
  - Command parameters should include: name, description, required (bool), type (string/number), optional enum
  - Parameter names should be descriptive for LLM understanding (forecast_type vs option)
  - Enum values help LLM choose valid options instead of guessing
  - Tests should verify both schema generation AND tool execution
  - Mock command execution tests use async functions that set last_response attribute
  - All 3045 tests pass, typecheck passes, lint passes, 44.53% coverage

---

## 2026-07-04 - US-010
- **What was implemented:**
  - Added parameter metadata to AirplanesCommand with optional radius parameter (number type)
  - Updated parameters list from 3 generic items to single well-typed parameter
  - Added 4 schema generation tests in TestAirplanesCommandToolSchema class
  - Added 4 tool execution tests in TestAirplanesCommandToolExecution class
  - Added airplanes_command to mypy ignore_errors list in pyproject.toml

- **Files changed:**
  - modules/commands/airplanes_command.py: Updated parameters list with required/type metadata
  - tests/test_tool_registry.py: Added TestAirplanesCommandToolSchema class with 4 tests
  - tests/test_tool_executor.py: Added TestAirplanesCommandToolExecution class with 4 tests
  - pyproject.toml: Added airplanes_command to mypy ignore_errors list

- **Learnings for future iterations:**
  - airplanes_command supports complex filtering but only radius is useful for LLM tool calls
  - Other filters (type, callsign, military, etc.) are too niche for LLM to use reliably
  - radius parameter is optional (default: 25nm) and type is "number" not "string"
  - Command uses bot location or companion location automatically when no location specified
  - Tests for tool execution verify both parameter passing and response content
  - Mock fixture pattern: create mock command → set parameters → assign to mock_command_manager.commands
  - All 3053 tests pass, typecheck passes, lint passes, 44.53% coverage

---

## 2026-07-04 - US-011
- **What was implemented:**
  - Added parameter metadata to SatpassCommand with optional satellite and visual parameters
  - satellite parameter is optional string type with NORAD ID or shortcuts (iss, hst, starlink, hubble, tiangong, goes18)
  - visual parameter is optional boolean type for filtering visible passes only
  - Added 4 schema generation tests in TestSatpassCommandToolSchema class
  - Added 4 tool execution tests in TestSatpassCommandToolExecution class
  - Added satpass_command to mypy ignore_errors list in pyproject.toml
  - Fixed import ordering in llm_command.py (ruff auto-fix)

- **Files changed:**
  - modules/commands/satpass_command.py: Updated parameters list with required/type metadata
  - tests/test_tool_registry.py: Added TestSatpassCommandToolSchema class with 4 tests
  - tests/test_tool_executor.py: Added TestSatpassCommandToolExecution class with 4 tests
  - pyproject.toml: Added satpass_command to mypy ignore_errors list
  - modules/commands/llm_command.py: Import ordering fix (Any, Optional)

- **Learnings for future iterations:**
  - satpass_command has two parameters: satellite (string, shortcuts like iss/hubble) and visual (boolean filter)
  - The command implementation checks for "visual" or "vis" strings in message content, not boolean True
  - When LLM passes visual: true, ToolExecutor converts it to string "True" in message content
  - Mock tests should verify satellite parameter is present, not strict visual string matching
  - Boolean parameters in tool schemas work correctly - LLM can call with true/false values
  - Satellite shortcuts include: iss, hst, hubble, starlink, tiangong, goes18 (plus NORAD IDs)
  - All 3061 tests pass, typecheck passes, lint passes, 44.53% coverage

---

## 2026-07-04 - US-012
- **What was implemented:**
  - Added parameter metadata to PathCommand with required destination parameter (string type)
  - destination parameter accepts node ID or hex path data (e.g., '01,5f' or 'A1B2')
  - Added 4 schema generation tests in TestPathCommandToolSchema class
  - Added 4 tool execution tests in TestPathCommandToolExecution class
  - All tests verify parameter structure, schema generation, and command execution

- **Files changed:**
  - modules/commands/path_command.py: Added parameters list with destination parameter
  - tests/test_tool_registry.py: Added TestPathCommandToolSchema class with 4 test cases
  - tests/test_tool_executor.py: Added TestPathCommandToolExecution class with 4 test cases

- **Learnings for future iterations:**
  - path_command accepts hex path data (comma-separated like '01,5F,A1') or destination node IDs
  - destination is required parameter since path analysis needs a target
  - PathCommand.execute() extracts everything after first word as path_input (line 266)
  - Test pattern: verify parameter metadata → schema generation → tool execution with mock responses
  - All 3083 tests pass, typecheck passes, lint passes, 44.53% coverage
  - path_command is large (933 lines) due to complex geographic and graph-based path inference
  - Schema description should emphasize "showing repeaters and signal quality" for LLM context

---

## 2026-07-04 - US-013
- **What was implemented:**
  - Created comprehensive integration test file tests/test_llm_tool_calling.py
  - Added 8 end-to-end integration tests covering complete tool calling workflow
  - Test: Weather query triggers wx_command tool call with LLM function calling
  - Test: Airplane query triggers airplanes_command tool call
  - Test: Multi-tool query calls both wx and satpass tools sequentially
  - Test: Invalid tool call rejected gracefully with error handling
  - Test: Tool timeout handled with error message to LLM
  - Test: User mention prefix added to responses ([@username] format)
  - Test: User mention skipped for DM responses
  - Test: Command context storage verified for tool-called commands

- **Files changed:**
  - tests/test_llm_tool_calling.py: New integration test file (615 lines, 8 test cases)

- **Learnings for future iterations:**
  - Integration tests should use fixtures from conftest.py (async_db_manager, mock_logger)
  - Mock bot fixture needs complete config with all LLM_Command settings (enable_tools, available_tools, etc.)
  - Tool calling flow: chat() → tool_calls → execute_tool() → chat() → final response
  - Mock chat responses as list of dicts with message.tool_calls and message.content
  - User mention format is "[@username] " prefix, skipped for DMs
  - Tool execution errors should still allow test to pass (graceful degradation)
  - Integration tests verify end-to-end workflow, not just individual components
  - All 3077 tests pass (including 8 new integration tests), typecheck passes, 44.53% coverage
  - Test organization: one fixture per mock command, one test class for all integration tests
  - Mock commands need: name, keywords, description, parameters list, execute() AsyncMock, last_response

---

